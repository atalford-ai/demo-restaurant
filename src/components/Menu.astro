---
import config from '../../site.json';
const menuCategories = config.menu;
---
<section id="menu" class="py-24 px-6 lg:px-12">
  <div class="container mx-auto">
    <div class="text-center mb-16 fade-in-up">
      <span class="badge badge-outline border-accent text-accent text-xs tracking-widest uppercase mb-4">The Menu</span>
      <h2 class="text-4xl md:text-5xl font-heading font-bold">What We're Serving</h2>
      <p class="text-base-content/60 mt-4 max-w-md mx-auto">Bold flavors, honest food. Every plate tells a story.</p>
    </div>

    <!-- Tabs -->
    <div class="flex justify-center mb-12">
      <div role="tablist" class="tabs tabs-boxed bg-base-200 p-1">
        {menuCategories.map((cat, i) => (
          <button
            role="tab"
            class:list={["tab font-medium text-sm", "menu-tab", { "tab-active": i === 0 }]}
            data-category={cat.category}
          >
            {cat.category}
          </button>
        ))}
      </div>
    </div>

    <!-- Menu Items -->
    {menuCategories.map((cat, i) => (
      <div
        class:list={["menu-panel grid grid-cols-1 md:grid-cols-2 gap-6 max-w-4xl mx-auto stagger-children", { "hidden": i !== 0 }]}
        data-panel={cat.category}
      >
        {cat.items.map((item) => (
          <div class="card bg-base-200 shadow-md hover:shadow-xl hover:-translate-y-1 transition-all duration-300">
            <div class="card-body p-6">
              <div class="flex justify-between items-start gap-4">
                <div class="flex-1">
                  <h3 class="font-heading font-semibold text-lg">{item.name}</h3>
                  <p class="text-base-content/60 text-sm mt-1 leading-relaxed">{item.description}</p>
                </div>
                <span class="text-accent font-heading font-bold text-xl whitespace-nowrap">${item.price}</span>
              </div>
            </div>
          </div>
        ))}
      </div>
    ))}
  </div>
</section>

<script is:inline>
(function() {
  var tabs = document.querySelectorAll('.menu-tab');
  var panels = document.querySelectorAll('.menu-panel');

  tabs.forEach(function(tab) {
    tab.addEventListener('click', function() {
      var category = this.dataset.category;

      // Update active tab
      tabs.forEach(function(t) { t.classList.remove('tab-active'); });
      this.classList.add('tab-active');

      // Show matching panel
      panels.forEach(function(p) {
        if (p.dataset.panel === category) {
          p.classList.remove('hidden');
        } else {
          p.classList.add('hidden');
        }
      });
    });
  });
})();
</script>
